@model IEnumerable<StayFit.Models.PostMessage>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/Admin_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Post_Message)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Post_Message)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Post_Message_Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Post_Message_Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Post_Message_Id })
        </td>
    </tr>
}

</table>
